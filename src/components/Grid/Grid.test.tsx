import React from "react";
import { render, screen } from "@testing-library/react";
import Grid from "./Grid";

test("renders grid", () => {
  render(<Grid size={10} />);

  // add grid tests here
});
